from ._base import Base


class Array(Base):
    """Array implements creation of structure arrays

    Array implements both static arrays and dynamic arrays.
    All arrays will be typed according to the type of their contents.
    Assignment to arrays will only succeed if the value has the same
    size as the array at the moment of assignment.
    """
    pass
